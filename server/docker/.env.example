# Docker Environment Configuration for Rubik Analytics
# Copy this file to .env and customize as needed
# Note: .env files are git-ignored for security

# ============================================
# Data Folder Configuration
# ============================================
# Data folder is AUTOMATICALLY DETECTED by docker-start scripts
# No configuration needed - the scripts find the data/ folder automatically
# 
# If you need to override (not recommended), set HOST_DATA_DIR to an absolute path:
#   Linux/Mac:   HOST_DATA_DIR=/home/user/rubik-analytics/data
#   Windows:     HOST_DATA_DIR=C:/Users/username/rubik-analytics/data
# Leave unset for automatic detection (recommended)
HOST_DATA_DIR=

# ============================================
# Backend Configuration (REQUIRED - NO DEFAULT PASSWORDS!)
# ============================================
# IMPORTANT: ALL THREE SECURITY KEYS BELOW ARE REQUIRED AND MUST BE SET!
# IMPORTANT: Default passwords are NOT allowed - the start scripts will reject them
# IMPORTANT: Generate unique, secure keys for each deployment

# JWT Secret Key (REQUIRED)
# Generate: openssl rand -base64 32
# Used for signing JWT authentication tokens
JWT_SECRET_KEY=

# JWT System Secret Key (REQUIRED)
# Generate: openssl rand -base64 32
# Used for system-level JWT operations
JWT_SYSTEM_SECRET_KEY=

# Encryption Key (REQUIRED)
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Used to encrypt connection credentials - MUST be a valid Fernet key (44 characters)
# Keep this key secure - if lost, encrypted data cannot be recovered!
ENCRYPTION_KEY=

# ============================================
# Backend Configuration (Optional)
# ============================================

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://frontend:3000,http://127.0.0.1:3000,http://rubik-frontend:3000

# TrueData Configuration
TRUEDATA_DEFAULT_AUTH_URL=https://auth.truedata.in/token
TRUEDATA_DEFAULT_WEBSOCKET_PORT=8086

# Timezone
TZ=Asia/Kolkata

# Admin User Creation (Optional - only used on first startup)
# Uncomment and set values to create admin user automatically
# ADMIN_USERNAME=admin
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=SecurePassword123!

# ============================================
# Frontend Configuration
# ============================================

# Backend API URL (use service name in Docker network)
NEXT_PUBLIC_API_URL=http://backend:8000