# Docker Environment Configuration for Rubik Analytics
# Copy this file to .env and customize as needed
# Note: .env files are git-ignored for security

# ============================================
# Data Folder Configuration (IMPORTANT)
# ============================================
# By default, Docker uses a relative path (../../data) from docker-compose.yml
# When transferring Docker to another machine, set HOST_DATA_DIR to an absolute path
# 
# Examples:
#   Linux/Mac:   HOST_DATA_DIR=/home/user/rubik-analytics/data
#   Windows:     HOST_DATA_DIR=C:/Users/username/rubik-analytics/data
#   Custom:      HOST_DATA_DIR=/absolute/path/to/your/data/folder
#
# Leave unset to use relative path (../../data) from docker-compose.yml location
HOST_DATA_DIR=

# ============================================
# Backend Configuration
# ============================================

# JWT Configuration (CHANGE IN PRODUCTION!)
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_SYSTEM_SECRET_KEY=your-system-secret-key-change-in-production

# Encryption Key (CHANGE IN PRODUCTION!)
# Generate new key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=jT7ACJPNHdp-IwKWVDto-vohgPGxwP_95sjBlgsr9Eg=

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://frontend:3000,http://127.0.0.1:3000,http://rubik-frontend:3000

# TrueData Configuration
TRUEDATA_DEFAULT_AUTH_URL=https://auth.truedata.in/token
TRUEDATA_DEFAULT_WEBSOCKET_PORT=8086

# Timezone
TZ=Asia/Kolkata

# Admin User Creation (Optional - only used on first startup)
# Uncomment and set values to create admin user automatically
# ADMIN_USERNAME=admin
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=SecurePassword123!

# ============================================
# Frontend Configuration
# ============================================

# Backend API URL (use service name in Docker network)
NEXT_PUBLIC_API_URL=http://backend:8000
