name: Build & Push Images to GHCR

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
    environment: rubik-env 

    permissions:
      contents: read
      packages: write

    steps:
      # 🚀 BUILD STARTED NOTIFICATION
      - name: Slack Notify Build Started
        run: |
          curl -X POST -H "Content-type: application/json" \
          --data '{
            "text": "🚀 *Rubik Build Started*\n• Branch: `${{ github.ref_name }}`\n• Commit: `${{ github.sha }}`\n• By: `${{ github.actor }}`"
          }' \
          ${{ secrets.SLACK_WEBHOOK_URL }}

      
      - name: Checkout code
        uses: actions/checkout@v4

        
      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io \
            -u ${{ github.actor }} --password-stdin

      - name: Build & push backend image
        run: |
          docker build \
            -f server/docker/backend.Dockerfile \
            -t ghcr.io/${{ github.repository_owner }}/rubik-backend:latest \
            .
          docker push ghcr.io/${{ github.repository_owner }}/rubik-backend:latest

      - name: Build & push frontend image
        run: |
          docker build \
            -f server/docker/frontend.Dockerfile \
            --build-arg NEXT_PUBLIC_API_URL=https://api.rubikanalytics.net \
            -t ghcr.io/${{ github.repository_owner }}/rubik-frontend:latest \
            .
          docker push ghcr.io/${{ github.repository_owner }}/rubik-frontend:latest

      # ✅ SUCCESS NOTIFICATION
      - name: Slack Notify Success
        if: success()
        run: |
          curl -X POST -H "Content-type: application/json" \
          --data '{
            "text": "✅ *Rubik Build Successful*\n• Backend & Frontend pushed to GHCR\n• Branch: `${{ github.ref_name }}`\n• Commit: `${{ github.sha }}`\n• By: `${{ github.actor }}`\n\n🚀 Watchtower will deploy shortly."
          }' \
          ${{ secrets.SLACK_WEBHOOK_URL }}

      # ❌ FAILURE NOTIFICATION
      - name: Slack Notify Failure
        if: failure()
        run: |
          curl -X POST -H "Content-type: application/json" \
          --data '{
            "text": "❌ *Rubik Build Failed*\n• Branch: `${{ github.ref_name }}`\n• Commit: `${{ github.sha }}`\n\n🔍 Check GitHub Actions logs immediately."
          }' \
          ${{ secrets.SLACK_WEBHOOK_URL }}
